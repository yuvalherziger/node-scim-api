{{- /*
This file intentionally contains a commented example for creating a Secret.
By default, the chart DOES NOT create a Secret. Create it separately or
enable secret management via values when desired.

To create the Secret outside Helm (recommended):
  kubectl create secret generic scim-api-secret \
    --from-literal=SCIM_BEARER_TOKEN='your_token_here'

If you prefer Helm to create it, copy this snippet into a new template or
uncomment and adapt for your needs.

apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  labels:
    {{- include "scim-api.labels" . | nindent 4 }}
type: Opaque
stringData:
  # The primary required secret variable
  # SCIM_BEARER_TOKEN: "change-me"
*/ -}}
